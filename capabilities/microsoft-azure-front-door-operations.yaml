naftiko: 1.0.0-alpha2
info:
  label: Azure Front Door REST API — Operations
  description: 'Azure Front Door REST API — Operations. 1 operations. Lead operation: Azure Front Door REST API List operations.
    Self-contained Naftiko capability covering one Microsoft Azure Front Door business surface.'
  tags:
  - Microsoft Azure Front Door
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_FRONT_DOOR_API_KEY: MICROSOFT_AZURE_FRONT_DOOR_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-azure-front-door-operations
    baseUri: https://management.azure.com
    description: Azure Front Door REST API — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.Cdn-operations
      path: /providers/Microsoft.Cdn/operations
      operations:
      - name: listoperations
        method: GET
        description: Azure Front Door REST API List operations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_FRONT_DOOR_API_KEY}}'
  exposes:
  - type: rest
    namespace: microsoft-azure-front-door-operations-rest
    port: 8080
    description: REST adapter for Azure Front Door REST API — Operations. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-cdn/operations
      name: providers-microsoft-cdn-operations
      description: REST surface for providers-Microsoft.Cdn-operations.
      operations:
      - method: GET
        name: listoperations
        description: Azure Front Door REST API List operations
        call: microsoft-azure-front-door-operations.listoperations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-azure-front-door-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure Front Door REST API — Operations. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: azure-front-door-rest-api
      description: Azure Front Door REST API List operations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-azure-front-door-operations.listoperations
      outputParameters:
      - type: object
        mapping: $.
